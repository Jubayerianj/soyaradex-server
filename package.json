{
  "name": "soyaradex-server",
  "version": "1.0.0",
  "private": true,
  "description": "Always-on settlement server for Soyara DEX: settles consensus-approved trades on GenLayer Bradbury with every browser tab closed.",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --env-file=.env src/index.js",
    "test": "node --test test/*.test.js"
  },
  "engines": {
    "node": ">=20.6"
  },
  "dependencies": {
    "genlayer-js": "1.1.8",
    "viem": "2.46.3"
  }
}
